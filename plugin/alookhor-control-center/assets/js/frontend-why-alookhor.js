(()=>{'use strict';function animateCounter(el){if(el.dataset.counted)return;el.dataset.counted='1';const raw=el.textContent.trim(),match=raw.match(/([0-9۰-۹]+)/);if(!match)return;const digits=match[1].replace(/[۰-۹]/g,d=>'۰۱۲۳۴۵۶۷۸۹'.indexOf(d)),target=Number(digits);if(!Number.isFinite(target))return;const prefix=raw.slice(0,match.index),suffix=raw.slice(match.index+match[1].length),start=performance.now(),duration=1100;function frame(now){const p=Math.min(1,(now-start)/duration),value=Math.round(target*(1-Math.pow(1-p,3)));el.textContent=prefix+value.toLocaleString('fa-IR')+suffix;if(p<1)requestAnimationFrame(frame)}requestAnimationFrame(frame)}function init(root){if(root.dataset.whyRuntime)return;root.dataset.whyRuntime='1';root.classList.add('awy-will-reveal');const reveal=()=>{root.classList.add('awy-visible');root.querySelectorAll('.awy-stats b').forEach(animateCounter)};if(!('IntersectionObserver'in window)){reveal();return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){reveal();observer.disconnect()}}),{threshold:.16});observer.observe(root)}const boot=()=>document.querySelectorAll('.alookhor-why').forEach(init);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot();new MutationObserver(boot).observe(document.documentElement,{childList:true,subtree:true})})();