name: Legacy disabled - Deploy PDP header verified package

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy v2 deployment is disabled. Canonical production deploy is deploy-pdp-header.yml."